# URL da HookSentry API — acessível pelo browser (login, register, invite)
NEXT_PUBLIC_API_URL=http://localhost:5000

# URL da HookSentry API — server-side only (proxy e refresh route handlers)
HOOKSENTRY_API_URL=http://localhost:5000

# URL pública do Grafana para links de logs
NEXT_PUBLIC_GRAFANA_URL=http://localhost:3001
